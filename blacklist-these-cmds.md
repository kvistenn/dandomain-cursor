rm -rf / rm -rf /* rm -rf ~/* rm -rf . rm -rf .. dd if=/dev/zero of=/dev/sda dd if=/dev/random of=/dev/sda mkfs.* shred wipefs `## Permission/Ownership Dangers` chmod -R 777 / chown -R root:root / chown -R nobody:nobody / chmod -R 000 / `## Fork Bombs & Resource Exhaustion` :(){ :|:& };: while true; do; done /dev/null > /dev/sda cat /dev/urandom > /dev/sda `## System Modification` mv / /dev/null > /dev/sda echo 1 > /proc/sys/kernel/panic kill -9 -1 killall -9 init systemctl stop * shutdown -h now (without explicit permission) reboot (without explicit permission) halt poweroff init 0 telinit 0 `## Kernel/System Core` rm -f /boot/* rm -f /etc/passwd rm -f /etc/shadow rm -f /etc/fstab modprobe -r * rmmod --all `## Network Attacks` ping -f (flood) hping3 (flood modes) slowloris any DDoS tool invocation arpspoof `## Package Manager Dangers` apt-get remove --purge * yum remove * pacman -Rdd * npm uninstall -g * pip uninstall -y * `## Wildcard Dangers in Critical Paths` rm -rf /bin/* rm -rf /sbin/* rm -rf /usr/* rm -rf /lib/* rm -rf /etc/* chmod -R * / chown -R * / `## Redirection to Critical Devices` > /dev/sda > /dev/null (when overwriting critical files) anything > /proc/sysrq-trigger `## Malicious Downloads & Execution` curl ... | bash wget ... | sh eval $(curl ...) bash <(curl ...)
